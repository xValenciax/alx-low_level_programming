#include "main.h"
/**
 * _abs - returns absolute value of an integer
 *
 * @n: integer to which the absolute value is to be returned
 *
 * Return: absolute value of n
 */

int _abs(int n)
{
if (n < 0)
return (n * -1);
return (n);
}
