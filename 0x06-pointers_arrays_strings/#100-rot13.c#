#include "main.h"

/**
 */

char *rot13(char *s)
{
  int i = 0, lower = 0;
  char tmp;

  while (s[i] != '\0')
    {
      while ((s[i] >= 'a' && s[i] <= 'z') || (s[i] >= 'A' && s[i] <= 'Z'))
	{
	  
	}
      i++;
    }
  
  return (s);
}
